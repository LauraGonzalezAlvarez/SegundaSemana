
	import java.util.ArrayList;
	import java.util.Scanner;
	
	/**
	 * Main principal 
	 *
	 * @author Laura Daniela Gonzalez Alvarez <lhaogonzaleza@gmail.com>
	 * @version 1.0.0 2022-06-05
	 * @since 1.0.0
	  */
		
public class PlanetarySystem {

	public static void main(String[] args) {
	
		  int opcion = 0;

	        /*
	         * Se crean los objetos planetarios y se agregan a la lista 
	         */
	        ArrayList<Planet> listPlanet = new ArrayList<Planet>();

	        listPlanets.add(new Earth());
	        listPlanets.add(new Jupiter());
	        listPlanets.add(new Saturn());
	        listPlanets.add(new Neptune());
	        listPlanets.add(new Mars());
	        listPlanets.add(new Uranus());
	        listPlanets.add(new Venus());
	        listPlanets.add(new Mercury());
	        listPlanets.add(new Earth("Tierra 2"));

	        /*
	         * Repetir el menú, hasta que el usuario presione la opción salir.
	         */
	        do {
	            Scanner entry = new Scanner(System.in);
	            System.out.println("╔═══ ☽ ≪ ☀✴°★°✴☀ ≫ ☾ ═══╗\n" +
	                    " Taller POO - Sistema solar\n" +
	                    "╚═══ ☽ ≪ ☀✴°★°✴☀ ≫ ☾ ═══╝\n" +
	                    "Por favor digite la opción que desee: \n" +
	                    "1. Listar cuerpos celestes. \n" +
	                    "2. Calcular atracción gravitatoria. \n" +
	                    "3. Salir.");
	            opcion = entry.nextInt();

	            switch (opcion) {
	                case 1:
	                    for (int i = 0; i < listPlanets.size(); i++) {
	                        System.out.println(listPlanets.get(i).informationPlanet());
	                    }
	                    break;

	                case 2:
	                    int index1, index2;
	                    Planet planet1, planet2;
	                    float distance;
	                    double force;

	                    for (int i = 0; i < listPlanets.size(); i++) {
	                        System.out.println(i + " " + listPlanets.get(i).getName());
	                    }

	                    Scanner entry2 = new Scanner(System.in);
	                    System.out.println("Digite el índice del primer planeta");
	                    index1 = entry2.nextInt();

	                    System.out.println("Digite el índice del segundo planeta");
	                    index2 = entry2.nextInt();

	                    planet1 = listPlanets.get(index1);
	                    planet2 = listPlanets.get(index2);

	                    System.out.println("Digite la distancia entre " + planet1.getName() + " y " + planet2.getName());
	                    distance = entry2.nextFloat();

	                    force = planet1.gravitationalAttraction(planet2, distance);
	                    System.out.println(
	                            "La atracción gravitatoria entre " +
	                                    planet1.getName() +
	                                    " y " +
	                                    planet2.getName() +
	                                    " es: " +
	                                    force
	                    );
	                    break;

	                case 3:
	                    System.exit(0);
	                    break;
	                /*
	                 * Mensaje en caso de digitar un número que no esté en el menú
	                 */
	                default:
	                    System.out.println("Número inválido");
	            }
	        } while (opcion != 4);
		
	}

}
